import { HeaderTitle, HeaderWrapper } from "@/components/FC/Header/styled";

export const Header = () => {
    return (
        <HeaderWrapper>
            <HeaderTitle>To Do List</HeaderTitle>
        </HeaderWrapper>
    );
};
